x <- c(1,2,3,4,5)

sum = 0 
for (i in x) {sum = sum + 1}
return (sum)



prime_check<- c(14){

if (prime_check <= 0)
  return(FALSE)
if( prime_check >1)
  for (i in 1:prime_check)
    if (prime_check(vector)%% i == 0)
    return(FALSE)}
else {return(TRUE)}



sq <- c()
j = sq ^ 0.5
return()



GCf <- c(a,b){
if (GCF[2]==0){return(GCF[1])}
else 
  return c(GCF[2], GCF[1]%%GCF[2])}

